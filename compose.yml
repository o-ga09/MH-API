version: "3.9"
services:
  mh-api-dbsrv01:
    image: mysql:8.0.29
    platform: linux/amd64
    container_name: mh-api-dbsrv01
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_USER: mh-api
      MYSQL_PASSWORD: P@ssw0rd
      MYSQL_DATABASE: mh-api
      MYSQL_ROOT_PASSWORD: pass
      MYSQL_TCP_PORT: 3306
      LANG: C.UTF-8
    command: mysqld --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    volumes:
      - mh-api-dbsrv01-data:/var/lib/mysql
      - ./db/mysql/conf.d/my.cnf:/etc/mysql/conf.d/my.cnf
      - ./db/mysql/sql:/docker-entrypoint-initdb.d
    ports:
      - "3306:3306"
  mh-api-srv:
    depends_on:
      - mh-api-dbsrv01
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    container_name: mh-api-srv
    environment:
      ENV: PROD
      PORT: 8080
      DATABASE_URL: mh-api:P@ssw0rd@tcp(mh-api-dbsrv01)/mh-api?charset=utf8&parseTime=True&loc=Local
      ALLOW_URL: "*"
      SENTRY_DSN: https://7f9767a1fa7379a3f8fcb8a618339b39@o4508995869343745.ingest.us.sentry.io/4509218119286784
    volumes:
      - ./:/app
    ports:
      - "8080:8080"
volumes:
  mh-api-dbsrv01-data:
